package com.eventsitemanager.domain.enumeration;

/**
 * Semantic kind for profile media assets (talks strip vs downloads).
 */
public enum ProfileMediaKind {
    DOCUMENT,
    VIDEO,
    PODCAST,
    PRESS,
    OTHER,
}
