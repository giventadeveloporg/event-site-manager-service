# Ensure Stripe library is loaded by base classloader (not restart classloader)
# This prevents classloader issues when introspecting StripePaymentAdapter
restart.exclude.stripe=/com/stripe/.*
# Also exclude payment adapters to ensure they use base classloader
restart.exclude.payment-adapters=com/nextjstemplate/service/payment/adapter/.*
