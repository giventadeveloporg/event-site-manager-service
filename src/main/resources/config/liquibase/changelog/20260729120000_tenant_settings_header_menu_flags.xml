<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Header menu visibility flags on tenant_settings.
      Nullable: null means use app defaults (existing items ON; news/downloads/links OFF).
    -->
    <changeSet id="20260729120000-1" author="system">
        <preConditions onFail="MARK_RAN">
            <tableExists tableName="tenant_settings"/>
            <not>
                <columnExists tableName="tenant_settings" columnName="show_header_home"/>
            </not>
        </preConditions>
        <sql><![CDATA[
            ALTER TABLE public.tenant_settings
                ADD COLUMN IF NOT EXISTS show_header_home boolean,
                ADD COLUMN IF NOT EXISTS show_header_about boolean,
                ADD COLUMN IF NOT EXISTS show_header_events boolean,
                ADD COLUMN IF NOT EXISTS show_header_features boolean,
                ADD COLUMN IF NOT EXISTS show_header_calendar boolean,
                ADD COLUMN IF NOT EXISTS show_header_gallery boolean,
                ADD COLUMN IF NOT EXISTS show_header_contact boolean,
                ADD COLUMN IF NOT EXISTS show_header_news boolean,
                ADD COLUMN IF NOT EXISTS show_header_downloads boolean,
                ADD COLUMN IF NOT EXISTS show_header_links boolean;
        ]]></sql>
    </changeSet>
</databaseChangeLog>
