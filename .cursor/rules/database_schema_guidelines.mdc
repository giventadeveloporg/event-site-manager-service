---
description: Database schema guidelines for PostgreSQL tables in this system; follow the conventions in the canonical schema file.
globs: src/main/java/**/domain/*.java, src/main/resources/config/liquibase/**/*.xml, src/main/resources/sqls/**/*.sql
alwaysApply: false
---

# Database Schema Guidelines

This rule defines the standard patterns for adding or modifying PostgreSQL tables in this project. The **canonical SQL reference** lives in the `mosc-temp` repo: `code_html_template/SQLS/Current_Sqls/Latest_Schema_Post__Blob_Claude_12.sql`. Liquibase changelogs in this service mirror per-table sequences for fresh installs.

## **ID and Primary Key**

- **Primary key column:** Always name the column `id`.
- **Type:** Use `bigint` (or `int8`) for all application table primary keys.
- **Per-table sequence:** Each application table owns `public.{table_name}_id_seq` (not a shared `sequence_generator`).
- **Definition:**
  ```sql
  CREATE SEQUENCE IF NOT EXISTS public.my_table_id_seq INCREMENT BY 1 ...;

  id bigint DEFAULT nextval('public.my_table_id_seq'::regclass) NOT NULL,
  CONSTRAINT my_table_pkey PRIMARY KEY (id)
  ```
- **Rationale:** Per-table sequences prevent cross-table ID inflation (e.g. executive committee ids no longer follow `event_media` max).

## **Join Tables (rel_*) — No Per-Table Sequence**

- **JHipster many-to-many join tables** (e.g. `rel_event_details__discount_codes`) use a **composite primary key** only — **no `id` column**, **no `{table}_id_seq`**.
- **Canonical schema** (`mosc-temp/.../Latest_Schema_Post__Blob_Claude_12.sql`) is correct: join table DDL only; sequence block goes `qr_code_usage_id_seq` → `tenant_organization_id_seq` with no `rel_*` sequence in between.
- **Liquibase** `create_per_table_id_sequences.sql` in this repo must mirror canonical (no `rel_event_details__discount_codes_id_seq`).
- **Legacy DBs** that ran an older Liquibase revision: run `src/main/resources/sqls/drop_orphan_join_table_sequences.sql` once, or rebuild from canonical schema (which now `DROP SEQUENCE` the orphan).

## **Spring Batch / Framework Sequences (Separate)**

- Framework: `batch_job_seq`, `batch_job_execution_seq`, `batch_step_execution_seq` — sync via `sync_spring_batch_sequences.sql` only.
- App audit: `batch_job_execution_log_id_seq` — separate from framework and from bulk app-table sync.
- Java `SequenceSynchronizationService` skips all `batch_*` and `rel_*` during bulk sync.

## **Multi-Tenancy**

- **Tenant column:** Include `tenant_id` on all tenant-scoped tables.
- **Type:** `character varying(255)` or `varchar(255)`; allow `NULL` only when the table is global (e.g. `databasechangelog`).
- **Unique constraints:** When a business key is unique per tenant, use a unique constraint on `(tenant_id, <other_columns>)`.

## **Timestamps**

- **Created/Updated:** Use `created_at` and `updated_at` for audit timestamps.
- **Type:** `timestamp without time zone DEFAULT now() NOT NULL` (or `timestamp DEFAULT now() NOT NULL`).
- **Trigger:** For tables that need automatic `updated_at` on row update, add a trigger that calls `public.update_updated_at_column()`.

## **Adding New Tables**

- **Schema file (mosc-temp):** Use **CREATE TABLE** only in the canonical schema; DROP in reverse dependency order at top.
- **Sequence:** Add `CREATE SEQUENCE IF NOT EXISTS public.<table>_id_seq` and `DEFAULT nextval('public.<table>_id_seq'::regclass)` on `id`.
- **Liquibase (this repo):** Add sequence + table to appropriate changelog or regenerate from canonical schema.
- **Java entity:** Unique `@SequenceGenerator` with `sequenceName = "public.<table>_id_seq"`, `allocationSize = 1`.
- **Post-import:** Run `sync_all_table_sequences.sql` (see `src/main/resources/sqls/`).

## **Per-Table Sequence Rules (Mandatory)**

- **Sequence increment:** `INCREMENT BY 1` on every `{table}_id_seq`.
- **Hibernate alignment:** Unique `@SequenceGenerator` name per entity class.
  ```java
  @SequenceGenerator(
      name = "myTableSeq",
      sequenceName = "public.my_table_id_seq",
      allocationSize = 1
  )
  ```
- **Runtime sync:** `SequenceSynchronizationService` discovers `*_id_seq` dynamically but **skips** `rel_*`, Spring Batch framework sequences, and tables without an `id` column; `POST /api/admin/sync-sequence` returns per-table status.
- **Spring Batch framework sequences** (`batch_job_seq`, etc.) are separate — never merge into app table sync.

- **Required post-import pattern (per table):**
  ```sql
  SELECT pg_catalog.setval(
      'public.my_table_id_seq',
      GREATEST(COALESCE((SELECT MAX(id) FROM public.my_table), 0), 1),
      true
  );
  ```
- **Deprecated:** `public.sequence_generator`, global GREATEST across all tables.

## **Foreign Keys**

- **Naming:** `CONSTRAINT fk_<short_name> FOREIGN KEY (column_id) REFERENCES public.<parent_table>(id) ON DELETE SET NULL` (or `ON DELETE CASCADE` where appropriate).
- **Column type:** Use `bigint` for foreign key columns referencing `id`.

## **Naming Conventions**

- **Tables:** `snake_case` (e.g. `news_article`, `executive_committee_team_members`).
- **Sequences:** `{table_name}_id_seq` (exceptions documented in schema: `discount_code_id_seq`, etc.).
- **Columns:** `snake_case`.
- **Constraints:** `tablename_pkey` for primary key; `ux_*` or `uq_*` for unique; `fk_*` for foreign key.

## **Reference Implementation**

- **Canonical schema:** `mosc-temp/code_html_template/SQLS/Current_Sqls/Latest_Schema_Post__Blob_Claude_12.sql`
- **Sync script (this repo):** [sync_all_table_sequences.sql](mdc:src/main/resources/sqls/sync_all_table_sequences.sql)
- **Liquibase:** [20260625120000_per_table_id_sequences.xml](mdc:src/main/resources/config/liquibase/changelog/20260625120000_per_table_id_sequences.xml)
- **Sequence alignment rule:** [sequence_id_alignment_and_import.mdc](mdc:.cursor/rules/sequence_id_alignment_and_import.mdc)

## **Summary Checklist for New Tables**

- [ ] `CREATE SEQUENCE public.<table>_id_seq INCREMENT BY 1`
- [ ] `id bigint DEFAULT nextval('public.<table>_id_seq'::regclass) NOT NULL` and `PRIMARY KEY (id)`
- [ ] `tenant_id` (if tenant-scoped)
- [ ] `created_at` / `updated_at` where applicable
- [ ] Java entity with unique `@SequenceGenerator` matching sequence name
- [ ] `allocationSize = 1` on all application entities
- [ ] Entry in `sync_all_table_sequences.sql` (or regenerate via mosc-temp generator script)
