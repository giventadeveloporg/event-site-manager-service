# Task ID: 3
# Title: Verify combined filter support with pagination and sorting
# Status: pending
# Dependencies: 1 ⏱️, 2 ⏱️
# Priority: high
# Description: Test if the GET /api/event-ticket-transactions endpoint supports combining multiple filters with pagination and sorting.
# Details:
- Test the endpoint with combined filters: eventId.equals, checkInStatus.equals, checkInTime range
- Test with pagination parameters (page, size)
- Test with sorting parameter (sort=checkInTime,desc)
- Verify that all filters work together correctly
- Document the results and response format
- Share findings with the frontend team

# Test Strategy:
Send requests with combined filters, pagination, and sorting, then verify the response contains correctly filtered, paginated, and sorted data by comparing with direct database queries.
