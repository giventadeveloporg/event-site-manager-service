# Task ID: 9
# Title: Add unique index on membership_subscription.stripe_subscription_id
# Status: pending
# Dependencies: None
# Priority: high
# Description: Create Liquibase migration to add unique index on stripe_subscription_id to prevent duplicate subscriptions.
# Details:
- Create Liquibase migration file in src/main/resources/config/liquibase/changelog/
- Add createIndex with unique=true on membership_subscription.stripe_subscription_id
- Include comment explaining purpose (race condition prevention)
- Pattern based on existing unique constraint on event_ticket_transaction.stripe_payment_intent_id

# Test Strategy:
Verify index is created correctly in database and prevents duplicate stripe_subscription_id values.
